# Cursor Rules

These files are adapted from `Jah-yee/cursor-rules` and rewritten as optional template seeds.

They are not active until copied into `.cursor/rules/` in a real project.

## Sample

```text
For a standard React app:
  copy project/00-core-engineering.mdc
  copy project/20-code-style.mdc
  copy project/40-testing-and-validation.mdc
  copy project/50-no-drive-by-refactor.mdc
  copy stacks/frontend-react.mdc
```
