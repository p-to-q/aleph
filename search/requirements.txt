# Aleph backend (search + live FastAPI).
#
# IMPORTANT: this search service is an MLX-backed local white-box route. Apple
# Silicon is the current maintainer path; upstream MLX now also supports Linux
# CUDA backends. Vercel still needs a separate reachable search service.
#
#   python3 -m venv search/.venv && source search/.venv/bin/activate
#   pip install -r search/requirements.txt
#   python3 search/preflight.py
#   python3 search/server.py            # -> http://localhost:8000

mlx-lm==0.31.3            # pulls mlx; Apple Silicon and upstream Linux CUDA path
sentence-transformers==5.4.1   # embedding distortion metric (pulls torch, transformers)
numpy<2                   # mlx 0.31 expects numpy 1.x
fastapi==0.136.1
uvicorn[standard]==0.40.0
