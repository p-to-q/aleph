# Security

Do not upload private model weights, API keys, user data, or non-public prompts to the repository.

Aleph may eventually support adversarial or reverse-prompt experiments. Keep them scoped to
approved models and datasets, and document whether a run is black-box, white-box, or simulated.
