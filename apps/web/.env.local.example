# Aleph web console — local environment
# Copy to .env.local and adjust.

# API base URL for the Aleph FastAPI service (apps/api).
# Start the API with:
#   PYTHONPATH=apps/api uvicorn aleph_api.main:app --app-dir apps/api --port 8010
VITE_API_BASE=http://127.0.0.1:8010
