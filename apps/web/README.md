# Archived Legacy Frontend

This directory preserves the old Vite/React Hackathon console as reference material.

It is not part of the root npm workspace, root scripts, Vercel deployment, or active product path.

Use `web/` for active frontend work.

